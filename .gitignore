# Xcode
build/
DerivedData/
xcuserdata/
*.xcuserstate
*.xcscmblueprint

# macOS
.DS_Store

# Swift Package Manager
.build/
.swiftpm/

# App Store Connect API credentials — never commit
fastlane/asc_key.json
fastlane/AuthKey_*.p8
fastlane/report.xml
fastlane/README.md
.wrangler/

# Client API keys (GIPHY) — bundled at build time, never committed
TeamRadio/Resources/Secrets.plist
